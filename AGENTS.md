# 静态站开发规则

先阅读 README.md、CONTRIBUTING.md，检查当前分支、提交和工作区。D23 决定内容和文案；本项目负责实现、验证和发布。

使用 Next.js 静态导出，本地版本化内容；不引入 WordPress、数据库或自建后台。共享组件唯一复用。表单接收采用 Web3Forms，但当前三页不含表单。

新任务从本地 develop 创建 codex/ 功能分支；不直接在 develop/main 开发。独立完成且验证通过的工作项一个 commit，明确暂存本任务文件并检查差异，不夹带他人修改。

源码变更运行适用测试，发布候选在 develop 上集成验证后才快进 main。测试结果绑定具体 SHA；失败不得发布。不覆盖历史证据，不伪造验收结果。

当前无远端和部署配置。push、远端创建、部署、DNS、凭据和旧 WordPress 环境操作需要对应任务授权。不泄露凭据，不强推或重写历史，不删除其他任务内容。

共享知识库只在用户明确要求时查询，不自动读写。迁移历史来源路径只用于追溯，不得成为运行依赖。
